defaults:
  - _self_
  - env: inverted_pendulum
  - alg: ppo

general:
  logdir: logs
  param_dir: params
  seed: 42
  debug: false
  no_wandb: true

wandb:
  project: fd_brax
  entity: mistry-lab
  group: ${alg.name}
  resume: False
