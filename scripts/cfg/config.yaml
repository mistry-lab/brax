defaults:
  - _self_
  - env: half_cheetah
  - alg: diffrl_shac

general:
  logdir: logs
  param_dir: params
  seed: 42
  debug: false
  no_wandb: true
  regular_env: false

wandb:
  project: sweep
  entity: mistry-lab
  group: ${alg.name}
  resume: False
