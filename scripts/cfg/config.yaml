defaults:
  - _self_
  - env: finger
  - alg: diffrl_shac

general:
  logdir: logs
  param_dir: params
  seed: 42
  debug: false
  no_wandb: true

wandb:
  project: fd_brax
  entity: mistry-lab
  group: ${alg.name}
  resume: False
