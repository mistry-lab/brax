defaults:
  - _self_
  - env: finger
  - alg: ppo

general:
  logdir: logs
  param_dir: params
  seed: 40
  debug: false
  no_wandb: true
  regular_env: false
  experiment_name: flip

wandb:
  project: dissertation
  entity: mistry-lab
  group: ${alg.name}
  resume: False
